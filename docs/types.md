# TypeScript Rules

## Use type instead of interface by default.

Use type for:

- Data models
- API request/response
- React props
- Zustand state
- Utility types
- Union types

Use interface only when:

- Extending object multiple times
- OOP class implements
- Declaration merging

Rule:
Prefer type over interface.
